# Apple-Homekit-Notify
HomeKit Notify lets you trigger push notifications from HomeKit (or any webhook-capable system) by registering a subscription and receiving a unique webhook URL. When that URL is called, a push notification is sent to the subscribed device.
